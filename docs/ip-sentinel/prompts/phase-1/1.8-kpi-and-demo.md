# 1.8 — Alert-noise KPI + correlation demo seed  ⭐

*Prepend [`00-shared-preamble.md`](../00-shared-preamble.md).*

## Context
Instruments the **primary success metric** (alert-noise reduction) and seeds the **flagship demo**
(cross-platform correlation). Together these make the MVP demonstrable and measurable.

## Scope
**In:** the noise KPI (compute + surface) and a reproducible demo scenario/fixture.
**Out:** reporting (3.4 consumes the KPI), the correlation engine itself (1.1).

## Dependencies
- 1.1 (incidents + signals), 0.9 (Fleet Overview surface), `alerts` table.

## Deliverables

### 1. Alert-noise reduction KPI
- Per tenant + period: **raw alert count** vs **correlated incident count**; ratio =
  `1 - incidents/raw_alerts` (guard divide-by-zero).
- Expose via API; surface on the **Fleet Overview** (and later in reports, 3.4).
- Backfillable over a period (uses stored `alerts` + `incidents`).

### 2. Cross-platform correlation demo seed
- A reproducible fixture/script: an IBM i subsystem outage (e.g. QINTER down) **plus** its Windows
  service fallout, emitted as breaches so **1.1 groups them into ONE incident** and **1.2 produces a
  root cause** (with a seeded runbook so RAG matches).
- Runnable as a **demo** and reused as an **integration test** asserting one incident + a narrative.

## Constraints / gotchas
- KPI is tenant-scoped; don't aggregate across tenants.
- The seed must be deterministic and self-contained (no live IBM i/Windows needed).

## Testing
- KPI: given N raw alerts collapsing to M incidents, the ratio is correct; divide-by-zero handled.
- Demo seed: running it yields exactly one cross-platform incident with a root-cause narrative.

## Acceptance
- [x] Noise-reduction KPI computes and displays on the Fleet Overview for a known period.
- [x] The seed reproducibly produces the single cross-platform incident used for the demo + as a test.

## References
- KPI consumed by 3.4 (reports). Demo exercises 1.1 + 1.2 end-to-end. Decision #22 (KPI), #25 (demo).

## Implementation notes
- `AlertNoiseKpiService.computeForPeriod` reads only `alerts`/`incidents` for one tenant+period
  (`ratio = 1 - incidents/rawAlerts`, 0.0 when rawAlerts is 0); `FleetHeadroomService` calls it with
  a rolling 30-day window and stamps `TenantFleetDto.noiseReductionRatio` (0-100%) alongside the
  existing headroom score, independently of the live Prometheus query path so a KPI failure never
  breaks headroom display. `/api/v1/kpi/alert-noise` exposes the same computation with an explicit
  period for backfill/3.4.
- `CrossPlatformDemoService` seeds one IBM i monitor + one Windows monitor linked by a
  `TopologyLink`, two breach alerts ~60s apart, and a `KbDoc(source="runbook")`/`KbChunk` pair, then
  drives the real (unmodified) 1.1 `CorrelationEngineService.processTenant` for
  `consecutiveBreachThreshold` cycles and 1.2 `TriageAgentService.triageIncident` - no seed-specific
  branching in either engine.
- Verification: `AlertNoiseKpiServiceTest` (ratio/divide-by-zero/tenant-isolation, pure unit) and the
  full default `mvn test` (244 tests) are green. `CrossPlatformDemoIntegrationTest` was added
  alongside the project's existing Docker/Spring-context integration tests (excluded from the
  default build per `pom.xml`'s `docker-integration-tests` profile) and was traced line-by-line
  against 1.1/1.2's real logic, but could not be run to a live green result in this session's shell:
  its `@SpringBootTest` context - like the pre-existing, untouched `TriageAgentGovernanceIntegrationTest`
  - fails to boot in this sandbox because `QueryFanoutExecutor`'s `java.net.http.HttpClient`
  construction hits `java.io.IOException: Unable to establish loopback connection` from the JDK's
  Windows `WEPollSelectorImpl` (an environment limitation the `pom.xml` comment already anticipates:
  "run explicitly with -Pdocker-integration-tests on a machine where `docker run` + a plain
  `Selector.open()` both succeed"). Confirm with:
  `mvn test -Pdocker-integration-tests -Dtest=CrossPlatformDemoIntegrationTest`.
- Also fixed, as a prerequisite for any of the above Spring-context tests to boot at all:
  `TeamsAuthTokenProvider`, `TeamsGraphClient`, `JiraClient`, `ServiceNowClient` (1.6/1.7) each had
  two constructors with neither `@Autowired`, so Spring required a default constructor that doesn't
  exist and failed context startup - unrelated to this chunk but blocking every full-context test.
