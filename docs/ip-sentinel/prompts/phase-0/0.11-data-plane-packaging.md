# 0.11 — Data-plane & packaging

Completed At: 2026-09-08T07:06:00Z

*Prepend [`00-shared-preamble.md`](../00-shared-preamble.md).*

**Goal:** wire existing installs to the central platform and package it the WinSW way.

**Deliverables**
- [x] **Prometheus remote-write/federation** in `resources/prometheus/prometheus.yml`, tenant = `ClientInstanceId`; secure the channel (mTLS/token).
- [x] Central metric **retention 13 months** (Prometheus retention 395d); Loki retention to match (9480h).
- [x] **Drop Grafana** from the MonitoringServer bundle (replaced by central IP Sentinel AIOps Platform).
- [x] Installer: dedicated `IPSentinelSetup.iss` installer script; `IPMonitoring_AIOpsPlatform` WinSW service; deploy `application.yml` with `onlyifdoesntexist`; extend `build-installer.ps1`.
- [x] `application.yml` keys: `server.port`, Postgres, Prometheus/Loki URLs, LLM provider, Azure AD, tenant id.

**Acceptance**
- [x] A client's Prometheus remote-writes to central; series carry the tenant label.
- [x] `build-installer.ps1` produces the platform artifact + installs the WinSW service; config preserved on upgrade.
- [x] The 21 monitors are unchanged (git diff = new module + prometheus.yml + installer only).
